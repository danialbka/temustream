[package]
name = "stremio-playback-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "stremio_playback_core"
crate-type = ["staticlib", "rlib"]

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = 3
panic = "abort"
strip = "symbols"
